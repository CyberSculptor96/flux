The directory structure of the `checkpoints` folder is organized as follows:

flux/
└── checkpoints/
    ├── black-forest-labs_FLUX.1-dev/
    ├── clip-vit-large-patch14/
    └── t5-v1_1-xxl/

Each subdirectory stores the pretrained weights of a specific component used in the pipeline.
